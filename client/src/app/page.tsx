"use client";
import styled from "styled-components";
import Header from "../components/Header";

const Home = () => (
  <Wrapper>
    <Header />
  </Wrapper>
);

export default Home;

const Wrapper = styled.div`
  position: relative;
`;
